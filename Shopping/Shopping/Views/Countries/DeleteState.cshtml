@model Shopping.Data.Entities.State

@{
    ViewData["Title"] = "Delete";
}

<h1>Borrar</h1>

<h3>¿Estas seguro de querer borrar?</h3>
<div>

    <partial name="_StateDetails" />
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Borrar" class="btn btn-outline-danger" /> |
        <a asp-action="Details" class="btn btn-outline-success" asp-route-id="@Model?.Country.Id">Regresar</a>
    </form>
</div>
